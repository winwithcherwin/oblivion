#!/bin/sh

cols="$(tput cols 2>/dev/null || echo 80)"
text="ｏｂｌｉｖｉｏｎ"
padding=$(( (cols - ${#text}) / 2 ))
[ "$padding" -lt 0 ] && padding=0
printf "\n\n\033[1;35m%*s%s\033[0m\n\n" "$padding" "" "$text"

