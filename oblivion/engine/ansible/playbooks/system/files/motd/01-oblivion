#!/bin/bash
# updated at: Mon Mar 24 04:03:03 PM UTC 2025
BANNER="ｏｂｌｉｖｉｏｎ"
WIDTH=$(tput cols 2>/dev/null || echo 80)
PADDING=$(( (WIDTH - ${#BANNER}) / 2 ))

echo ""
printf "%*s\n" $((PADDING + ${#BANNER})) "$BANNER"
echo ""

